# FlowIntel Configuration
COMPOSE_PROJECT_NAME=flowintel

# Application
APP_PORT=7006
JWT_SECRET=changeme_SuperSecretRWAndomString123!

# Database (Postgres)
# Note: Host mapping is disabled by default in docker-compose.yml to avoid conflicts
POSTGRES_USER=flowintel
POSTGRES_PASSWORD=changeme
POSTGRES_DB=flowintel
# POSTGRES_PORT=5432 

# Cache (Valkey)
# VALKEY_PORT=6379
VALKEY_IP=valkey

# Initial Admin User (Created on first run only)
INIT_ADMIN_EMAIL=admin@admin.admin
INIT_ADMIN_PASSWORD=admin

# MISP Modules (Sharing with MISP Stack)
MISP_MODULES_URL=http://misp-modules-shared:6666
