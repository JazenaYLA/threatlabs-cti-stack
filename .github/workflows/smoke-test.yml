name: Smoke Test
on: [push]

jobs:
  test-connection:
    runs-on: local-shell  # Matches the label in your compose.yaml
    steps:
      - name: Say Hello
        run: echo "✅ The Runner is ALIVE on 192.168.3.169!"

      - name: Check User
        run: whoami && id

      - name: Check Docker Access
        run: docker ps