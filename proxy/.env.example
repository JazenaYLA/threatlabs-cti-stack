# ── Proxy Stack Environment Variables ──

# Caddy LXC IP address (Admin API target)
CADDY_LXC_IP=192.168.x.x

# Dockge host IP (for CORS origin)
DOCKGE_HOST=192.168.x.x

# Exposed ports
CADDYMANAGER_PORT=8190
CADDYGEN_PORT=8189

# JWT secret for CaddyManager auth
# IMPORTANT: Change this to a strong random value! (e.g., openssl rand -hex 32)
JWT_SECRET=ChangeMe_JWT_Secret
