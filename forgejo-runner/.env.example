# ─── Forgejo Runner Configuration ───────────────────────────────
# Always use Caddy domain names (e.g. forgejo.lab.local) instead of
# raw IPs. The entrypoint script will auto-detect and re-register
# if this URL changes (VLAN migration, IP change, etc.)
GITEA_INSTANCE_URL=http://forgejo.lab.local

# Get this from: Forgejo → Site Administration → Actions → Runners → Create new Runner
GITEA_RUNNER_TOKEN=ChangeMe_Token_From_Forgejo
