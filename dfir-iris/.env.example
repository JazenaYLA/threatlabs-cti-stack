# DFIR-IRIS Configuration
COMPOSE_PROJECT_NAME=dfir-iris

# -- LISTENING PORT (HTTPS)
IRIS_HTTPS_PORT=4433

# -- NGINX / CERTIFICATES
SERVER_NAME=iris.local
KEY_FILENAME=iris_dev_key.pem
CERT_FILENAME=iris_dev_cert.pem

# -- DATABASE (internal Postgres 12 — NOT shared infra)
# ⚠️  CRITICAL: Change these for production!
#     Generate with: openssl rand -hex 30
#     Do NOT use special chars: + / # @
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_ADMIN_USER=raptor
POSTGRES_ADMIN_PASSWORD=changeme
POSTGRES_DB=iris_db

# -- IRIS APPLICATION
# ⚠️  CRITICAL: Change these for production!
#     Generate with: openssl rand -hex 30
DOCKERIZED=1
IRIS_SECRET_KEY=changeme
IRIS_SECURITY_PASSWORD_SALT=changeme

# -- WORKER
# -- WORKER
CELERY_BROKER=amqp://iris-rabbitmq

# -- UI / CUSTOMIZATION
#IRIS_ORGANISATION_NAME=My Org
#IRIS_LOGIN_BANNER_TEXT=Authorized Personnel Only

# -- AUTHENTICATION
IRIS_AUTHENTICATION_TYPE=local

# -- OPTIONAL: Initial admin credentials (only applied on first boot)
#    If not set, a random password is generated and printed in logs.
#    Password must meet complexity requirements or login will fail.
#IRIS_ADM_PASSWORD=MySuperAdminPassword!
#IRIS_ADM_API_KEY=changeme
#IRIS_ADM_EMAIL=admin@localhost
#IRIS_ADM_USERNAME=administrator
